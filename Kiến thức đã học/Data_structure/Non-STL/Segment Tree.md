# Segment Tree

## Mục tiêu
- Query đoạn và update nhanh trên mảng động.

## Độ phức tạp
- Build `O(n)`.
- Query/update `O(log n)`.

## Dạng phổ biến
- Point update + range query.
- Range update + range query (lazy propagation).
