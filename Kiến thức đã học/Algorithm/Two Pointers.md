# Two Pointers

## Dạng chính
1. Sliding window trên một mảng.
2. Hai đầu mảng đã sort.
3. Hai con trỏ trên hai mảng.
4. Slow/Fast pointer tìm chu trình.

## Nhận diện
- Có tính đơn điệu khi tăng/giảm cửa sổ.
- Cần tối ưu từ `O(n^2)` xuống gần `O(n)`.
