

  

## 1. Kí hiệu nhanh

  

- Điểm và vector viết thống nhất trong $\mathbb R^n$:

  $$

  A(a_1,\dots,a_n),\qquad

  \vec{AB}=B-A=(b_1-a_1,\dots,b_n-a_n).

  $$

- Độ dài vector và khoảng cách:

  $$

  |\vec u|=\sqrt{u_1^2+\cdots+u_n^2},\qquad

  AB=|\vec{AB}|.

  $$

- Trung điểm:

  $$

  M=\frac{A+B}{2}.

  $$

- Điểm chia đoạn $AB$ theo tỉ số $AM:MB=m:n$:

  $$

  M=\frac{nA+mB}{m+n}.

  $$

- Trọng tâm của các điểm $A_1,A_2,\dots,A_k$:

  $$

  G=\frac{A_1+A_2+\cdots+A_k}{k}.

  $$

- Tích vô hướng:

  $$

  \vec u\cdot\vec v=u_1v_1+\cdots+u_nv_n

  =|\vec u||\vec v|\cos\theta.

  $$

- Góc giữa hai vector:

  $$

  \cos\theta=\frac{\vec u\cdot\vec v}{|\vec u||\vec v|}.

  $$

- Hai vector vuông góc:

  $$

  \vec u\cdot \vec v=0.

  $$

- Hai vector cùng phương:

  $$

  \vec u=k\vec v\quad(k\in\mathbb R).

  $$

  

## 2. Hình học phẳng cơ bản

  

### 2.1. Tam giác

  

Với tam giác $ABC$:

  

- Cạnh:

  $$

  a=BC,\qquad b=CA,\qquad c=AB.

  $$

- Nửa chu vi:

  $$

  p=\frac{a+b+c}{2}.

  $$

- Diện tích:

  $$

  S=\frac12 ah_a=\frac12 bc\sin A.

  $$

- Công thức Heron:

  $$

  S=\sqrt{p(p-a)(p-b)(p-c)}.

  $$

- Định lí cos:

  $$

  a^2=b^2+c^2-2bc\cos A.

  $$

- Định lí sin:

  $$

  \frac{a}{\sin A}=\frac{b}{\sin B}=\frac{c}{\sin C}=2R.

  $$

- Bán kính đường tròn ngoại tiếp:

  $$

  R=\frac{abc}{4S}.

  $$

- Bán kính đường tròn nội tiếp:

  $$

  r=\frac{S}{p}.

  $$

- Trung tuyến từ $A$:

  $$

  m_a^2=\frac{2b^2+2c^2-a^2}{4}.

  $$

- Phân giác trong từ $A$:

  $$

  l_a^2=bc\left(1-\frac{a^2}{(b+c)^2}\right).

  $$

- Đường cao:

  $$

  h_a=\frac{2S}{a}.

  $$

  

### 2.2. Tam giác đặc biệt

  

- Tam giác vuông:

  $$

  a^2+b^2=c^2.

  $$

- Diện tích tam giác vuông:

  $$

  S=\frac12 ab.

  $$

- Đường cao từ đỉnh vuông xuống cạnh huyền:

  $$

  h^2=mn,\qquad a^2=cm,\qquad b^2=cn,

  $$

  trong đó $c$ là cạnh huyền, $m,n$ là hai đoạn trên cạnh huyền.

- Tam giác đều cạnh $a$:

  $$

  h=\frac{a\sqrt3}{2},\qquad S=\frac{a^2\sqrt3}{4},\qquad R=\frac{a\sqrt3}{3},\qquad r=\frac{a\sqrt3}{6}.

  $$

  

### 2.3. Tứ giác và đa giác

  

- Hình bình hành:

  $$

  S=ah=ab\sin\theta.

  $$

- Hình chữ nhật:

  $$

  S=ab,\qquad d=\sqrt{a^2+b^2}.

  $$

- Hình vuông:

  $$

  S=a^2,\qquad d=a\sqrt2.

  $$

- Hình thoi:

  $$

  S=ah=\frac12 d_1d_2.

  $$

- Hình thang:

  $$

  S=\frac{(a+b)h}{2}.

  $$

- Tứ giác nội tiếp:

  $$

  A+C=180^\circ,\qquad B+D=180^\circ.

  $$

- Định lí Ptolemy cho tứ giác nội tiếp $ABCD$:

  $$

  AC\cdot BD=AB\cdot CD+BC\cdot AD.

  $$

- Đa giác đều $n$ cạnh, cạnh $a$:

  $$

  P=na,\qquad S=\frac12 Pr=\frac{na^2}{4\tan(\pi/n)}.

  $$

  

### 2.4. Đường tròn

  

- Chu vi, diện tích:

  $$

  C=2\pi R,\qquad S=\pi R^2.

  $$

- Độ dài cung góc ở tâm $\alpha$ rad:

  $$

  l=R\alpha.

  $$

- Diện tích quạt tròn:

  $$

  S_q=\frac12 R^2\alpha.

  $$

- Dây cung chắn góc ở tâm $\alpha$:

  $$

  d=2R\sin\frac{\alpha}{2}.

  $$

- Góc nội tiếp bằng nửa góc ở tâm cùng chắn một cung:

  $$

  \widehat{AMB}=\frac12\widehat{AOB}.

  $$

- Lực của điểm $M$ đối với đường tròn tâm $O$, bán kính $R$:

  $$

  \operatorname{Pow}(M)=MO^2-R^2.

  $$

- Nếu hai cát tuyến từ $M$:

  $$

  MA\cdot MB=MC\cdot MD.

  $$

- Nếu tiếp tuyến $MT$ và cát tuyến $MAB$:

  $$

  MT^2=MA\cdot MB.

  $$

  

## 3. Tọa độ trong mặt phẳng $Oxy$

  

### 3.1. Điểm và vector

  

Các công thức khoảng cách, trung điểm, chia đoạn, trọng tâm, tích vô hướng và góc giữa hai vector dùng thống nhất theo dạng vector ở mục 1.

  

### 3.2. Đường thẳng

  

- Dạng tổng quát:

  $$

  ax+by+c=0,\qquad \vec n=(a,b).

  $$

- Vector chỉ phương của đường thẳng:

  $$

  \vec u=(b,-a)\quad \text{hoặc}\quad \vec u=(-b,a).

  $$

- Qua điểm $M_0(x_0,y_0)$, có vector chỉ phương $\vec u=(u_1,u_2)$:

  $$

  \frac{x-x_0}{u_1}=\frac{y-y_0}{u_2}.

  $$

- Dạng tham số:

  $$

  \begin{cases}

  x=x_0+u_1t,\\

  y=y_0+u_2t.

  \end{cases}

  $$

- Dạng hệ số góc:

  $$

  y=kx+m.

  $$

- Khoảng cách từ điểm $M(x_0,y_0)$ đến đường thẳng $ax+by+c=0$:

  $$

  d(M,\Delta)=\frac{|ax_0+by_0+c|}{\sqrt{a^2+b^2}}.

  $$

- Góc giữa hai đường thẳng có vector pháp tuyến $\vec n_1,\vec n_2$:

  $$

  \cos\varphi=\frac{|\vec n_1\cdot\vec n_2|}{|\vec n_1||\vec n_2|}.

  $$

- Hai đường thẳng song song:

  $$

  a_1b_2-a_2b_1=0.

  $$

- Hai đường thẳng vuông góc:

  $$

  a_1a_2+b_1b_2=0.

  $$

  

### 3.3. Diện tích bằng tọa độ

  

- Diện tích tam giác:

  $$

  S_{ABC}=\frac12\left|\det(\vec{AB},\vec{AC})\right|.

  $$

- Diện tích đa giác $A_1A_2...A_n$:

  $$

  S=\frac12\left|\sum_{i=1}^{n}(x_iy_{i+1}-y_ix_{i+1})\right|,

  $$

  với $A_{n+1}=A_1$.

  

### 3.4. Đường tròn

  

- Dạng chuẩn:

  $$

  (x-a)^2+(y-b)^2=R^2.

  $$

- Dạng tổng quát:

  $$

  x^2+y^2+Dx+Ey+F=0.

  $$

- Tâm và bán kính:

  $$

  I\left(-\frac D2,-\frac E2\right),\qquad

  R=\sqrt{\frac{D^2+E^2}{4}-F}.

  $$

- Tiếp tuyến tại $M(x_0,y_0)$ của đường tròn tâm $I(a,b)$:

  $$

  (x_0-a)(x-x_0)+(y_0-b)(y-y_0)=0.

  $$

  

### 3.5. Conic thường gặp

  

- Elip:

  $$

  \frac{x^2}{a^2}+\frac{y^2}{b^2}=1,\qquad c^2=a^2-b^2,\qquad e=\frac ca.

  $$

- Hyperbol:

  $$

  \frac{x^2}{a^2}-\frac{y^2}{b^2}=1,\qquad c^2=a^2+b^2,\qquad e=\frac ca.

  $$

- Parabol:

  $$

  y^2=2px.

  $$

  

## 4. Vector trong mặt phẳng và không gian

  

### 4.1. Các phép toán vector

  

- Cộng, trừ:

  $$

  \vec u\pm\vec v=(u_1\pm v_1,\dots,u_n\pm v_n).

  $$

- Nhân với số:

  $$

  k\vec u=(ku_1,\dots,ku_n).

  $$

- Hình chiếu của $\vec u$ lên $\vec v$:

  $$

  \operatorname{proj}_{\vec v}\vec u=\frac{\vec u\cdot\vec v}{|\vec v|^2}\vec v.

  $$

- Thành phần vuông góc với $\vec v$:

  $$

  \vec u_\perp=\vec u-\operatorname{proj}_{\vec v}\vec u.

  $$

  

### 4.2. Tích có hướng trong không gian

  

Với $\vec u=(u_1,u_2,u_3)$, $\vec v=(v_1,v_2,v_3)$:

  

$$

\vec u\times\vec v=

\begin{vmatrix}

\vec i&\vec j&\vec k\\

u_1&u_2&u_3\\

v_1&v_2&v_3

\end{vmatrix}.

$$

  

- Dạng khai triển:

  $$

  \vec u\times\vec v=

  (u_2v_3-u_3v_2,\ u_3v_1-u_1v_3,\ u_1v_2-u_2v_1).

  $$

- Vuông góc với hai vector thành phần:

  $$

  \vec u\times\vec v\perp\vec u,\qquad

  \vec u\times\vec v\perp\vec v.

  $$

- Đổi thứ tự được vector đối:

  $$

  \vec u\times\vec v=-(\vec v\times\vec u).

  $$

- Độ lớn:

  $$

  |\vec u\times\vec v|=|\vec u||\vec v|\sin\theta.

  $$

- Tính chất biến đổi:

  $$

  \vec u\times\vec v=(\vec u+m\vec v)\times\vec v

  =\vec u\times(\vec v+k\vec u),\qquad m,k\in\mathbb R.

  $$

- Diện tích hình bình hành:

  $$

  S=|\vec u\times\vec v|.

  $$

- Diện tích tam giác:

  $$

  S=\frac12|\vec u\times\vec v|.

  $$

- Hai vector cùng phương:

  $$

  \vec u\times\vec v=\vec 0.

  $$

  Với $\vec u=(a,b,c)$, $\vec v=(a',b',c')$:

  $$

  \vec u\parallel\vec v

  \Leftrightarrow \exists k\in\mathbb R:\vec u=k\vec v

  \Leftrightarrow a=ka',\ b=kb',\ c=kc'.

  $$

  

### 4.3. Tích hỗn tạp

  

$$

[\vec u,\vec v,\vec w]=\vec u\cdot(\vec v\times\vec w).

$$

  

- Thể tích hình hộp:

  $$

  V=|[\vec u,\vec v,\vec w]|.

  $$

- Thể tích tứ diện:

  $$

  V=\frac16|[\vec u,\vec v,\vec w]|.

  $$

- Ba vector đồng phẳng:

  $$

  [\vec u,\vec v,\vec w]=0.

  $$

  

### 4.4. Tính chất tuyến tính thường dùng

  

Các tính chất dưới đây giúp rút gọn nhanh biểu thức vector, diện tích, thể tích và điều kiện hình học.

  

- Cộng vector và nhân với số là tuyến tính:

  $$

  k(\vec u+\vec v)=k\vec u+k\vec v,\qquad

  (k+m)\vec u=k\vec u+m\vec u.

  $$

- Tích vô hướng tuyến tính theo từng biến:

  $$

  (\alpha\vec u+\beta\vec v)\cdot\vec w

  =\alpha(\vec u\cdot\vec w)+\beta(\vec v\cdot\vec w).

  $$

  $$

  \vec u\cdot(\alpha\vec v+\beta\vec w)

  =\alpha(\vec u\cdot\vec v)+\beta(\vec u\cdot\vec w).

  $$

- Tích có hướng song tuyến tính:

  $$

  (\alpha\vec u+\beta\vec v)\times\vec w

  =\alpha(\vec u\times\vec w)+\beta(\vec v\times\vec w).

  $$

  $$

  \vec u\times(\alpha\vec v+\beta\vec w)

  =\alpha(\vec u\times\vec v)+\beta(\vec u\times\vec w).

  $$

- Tích hỗn tạp tuyến tính theo từng biến:

  $$

  [\alpha\vec u+\beta\vec v,\vec a,\vec b]

  =\alpha[\vec u,\vec a,\vec b]+\beta[\vec v,\vec a,\vec b].

  $$

  Tương tự cho biến thứ hai và thứ ba.

- Định thức tuyến tính theo từng cột hoặc từng hàng:

  $$

  \det(\alpha\vec u+\beta\vec v,\vec w)

  =\alpha\det(\vec u,\vec w)+\beta\det(\vec v,\vec w).

  $$

  $$

  \det(\alpha\vec u+\beta\vec v,\vec a,\vec b)

  =\alpha\det(\vec u,\vec a,\vec b)+\beta\det(\vec v,\vec a,\vec b).

  $$

- Hình chiếu lên một vector cố định là phép tuyến tính:

  $$

  \operatorname{proj}_{\vec v}(\alpha\vec u+\beta\vec w)

  =\alpha\operatorname{proj}_{\vec v}\vec u+\beta\operatorname{proj}_{\vec v}\vec w.

  $$

- Với ánh xạ affine $T(X)=LX+B$, hiệu hai điểm chỉ còn phần tuyến tính:

  $$

  \overrightarrow{T(A)T(B)}=L\vec{AB}.

  $$

- Tổ hợp affine giữ quan hệ điểm:

  $$

  \lambda_1+\cdots+\lambda_k=1

  \Rightarrow

  T\left(\sum_{i=1}^k\lambda_iA_i\right)

  =\sum_{i=1}^k\lambda_iT(A_i).

  $$

- Khoảng cách có hướng từ điểm $M$ đến mặt phẳng $(P):\vec n\cdot X+c=0$ là hàm affine theo $M$:

  $$

  \delta(M,(P))=\frac{\vec n\cdot M+c}{|\vec n|}.

  $$

  Nếu $M=\alpha A+\beta B$ thì:

  $$

  \delta(M,(P))=\alpha\delta(A,(P))+\beta\delta(B,(P))

  +\frac{1-\alpha-\beta}{|\vec n|}c.

  $$

  Đặc biệt, nếu $M=\lambda_1A_1+\cdots+\lambda_kA_k$ và $\lambda_1+\cdots+\lambda_k=1$ thì:

  $$

  \delta(M,(P))=\lambda_1\delta(A_1,(P))+\cdots+\lambda_k\delta(A_k,(P)).

  $$

- Lưu ý: độ dài, khoảng cách không hướng, góc và diện tích có dấu tuyệt đối không tuyến tính. Chỉ phần vector, khoảng cách có hướng, tích vô hướng, tích có hướng, tích hỗn tạp hoặc định thức trước khi lấy trị tuyệt đối mới có tính tuyến tính.

  

## 5. Hình học không gian

  

### 5.1. Công thức thể tích và diện tích

  

- Lăng trụ:

  $$

  V=S_{\text{đáy}}h.

  $$

- Hình hộp chữ nhật:

  $$

  V=abc,\qquad d=\sqrt{a^2+b^2+c^2}.

  $$

- Hình lập phương:

  $$

  V=a^3,\qquad S_{\text{tp}}=6a^2,\qquad d=a\sqrt3.

  $$

- Trụ:

  $$

  V=\pi R^2h,\qquad S_{\text{xq}}=2\pi Rh,\qquad S_{\text{tp}}=2\pi R(h+R).

  $$

- Nón:

  $$

  V=\frac13\pi R^2h,\qquad S_{\text{xq}}=\pi Rl,\qquad S_{\text{tp}}=\pi R(l+R).

  $$

- Cầu:

  $$

  V=\frac43\pi R^3,\qquad S=4\pi R^2.

  $$

  

### 5.2. Thể tích khối chóp và tỉ lệ thể tích

  

- Khối chóp:

  $$

  V=\frac13S_{\text{đáy}}h.

  $$

- Khối tứ diện $ABCD$:

  $$

  V_{ABCD}=\frac16\left|\vec{AB}\cdot(\vec{AC}\times\vec{AD})\right|.

  $$

- Hai khối chóp có cùng chiều cao:

  $$

  \frac{V_1}{V_2}=\frac{S_1}{S_2}.

  $$

- Hai khối chóp có cùng diện tích đáy:

  $$

  \frac{V_1}{V_2}=\frac{h_1}{h_2}.

  $$

- Hai khối chóp có cùng đỉnh và đáy nằm trong cùng một mặt phẳng:

  $$

  \frac{V_{S.A_1A_2\dots A_n}}{V_{S.B_1B_2\dots B_m}}

  =\frac{S_{A_1A_2\dots A_n}}{S_{B_1B_2\dots B_m}}.

  $$

- Nếu $D\in BC$ trong khối chóp $S.ABC$ thì:

  $$

  \frac{V_{S.ABD}}{V_{S.ACD}}=\frac{BD}{DC}.

  $$

- Nếu $I$ nằm trong tam giác $ABC$ thì:

  $$

  V_{S.IBC}:V_{S.ICA}:V_{S.IAB}

  =

  S_{IBC}:S_{ICA}:S_{IAB}.

  $$

- Với chóp đáy tứ giác $S.ABCD$, chọn một đường chéo đáy để tách:

  $$

  V_{S.ABCD}=V_{S.ABC}+V_{S.ACD}.

  $$

  Vì hai khối $S.ABC$ và $S.ACD$ có cùng chiều cao từ $S$ xuống $(ABCD)$:

  $$

  \frac{V_{S.ABC}}{V_{S.ACD}}=\frac{S_{ABC}}{S_{ACD}}.

  $$

- Nếu $I$ nằm trong tứ giác $ABCD$ thì:

  $$

  V_{S.IAB}:V_{S.IBC}:V_{S.ICD}:V_{S.IDA}

  =

  S_{IAB}:S_{IBC}:S_{ICD}:S_{IDA}.

  $$

- Nếu tứ giác $ABCD$ là hình bình hành và $O$ là giao điểm hai đường chéo thì:

  $$

  V_{S.OAB}=V_{S.OBC}=V_{S.OCD}=V_{S.ODA}

  =\frac14V_{S.ABCD}.

  $$

- Nếu $A'\in SA$, $B'\in SB$, $C'\in SC$ thì:

  $$

  \frac{V_{S.A'B'C'}}{V_{S.ABC}}

  =

  \frac{SA'}{SA}\cdot\frac{SB'}{SB}\cdot\frac{SC'}{SC}.

  $$

- Với chóp $S.ABCD$, nếu $A'\in SA$, $B'\in SB$, $C'\in SC$, $D'\in SD$, đặt:

  $$

  x_A=\frac{SA'}{SA},\quad x_B=\frac{SB'}{SB},\quad

  x_C=\frac{SC'}{SC},\quad x_D=\frac{SD'}{SD}.

  $$

  Khi dùng đường chéo $AC$ để tách đáy:

  $$

  V_{S.A'B'C'D'}=V_{S.A'B'C'}+V_{S.A'C'D'}.

  $$

  Do đó:

  $$

  \frac{V_{S.A'B'C'D'}}{V_{S.ABCD}}

  =

  \frac{x_Ax_Bx_CV_{S.ABC}+x_Ax_Cx_DV_{S.ACD}}

  {V_{S.ABC}+V_{S.ACD}}.

  $$

  Nếu $S_{ABC}=S_{ACD}$, chẳng hạn $ABCD$ là hình bình hành, thì:

  $$

  \frac{V_{S.A'B'C'D'}}{V_{S.ABCD}}

  =\frac{x_Ax_C(x_B+x_D)}2.

  $$

- Nếu $(A'B'C')\parallel(ABC)$, với

  $$

  \frac{SA'}{SA}=\frac{SB'}{SB}=\frac{SC'}{SC}=k,

  $$

  thì:

  $$

  \frac{S_{A'B'C'}}{S_{ABC}}=k^2,\qquad

  \frac{V_{S.A'B'C'}}{V_{S.ABC}}=k^3.

  $$

  Phần chóp cụt giữa hai đáy có thể tích:

  $$

  V_{\text{cụt}}=(1-k^3)V_{S.ABC}.

  $$

- Tương tự, nếu $(A'B'C'D')\parallel(ABCD)$ trong chóp $S.ABCD$ và

  $$

  \frac{SA'}{SA}=\frac{SB'}{SB}=\frac{SC'}{SC}=\frac{SD'}{SD}=k,

  $$

  thì:

  $$

  \frac{S_{A'B'C'D'}}{S_{ABCD}}=k^2,\qquad

  \frac{V_{S.A'B'C'D'}}{V_{S.ABCD}}=k^3,

  $$

  và phần chóp cụt có thể tích:

  $$

  V_{\text{cụt}}=(1-k^3)V_{S.ABCD}.

  $$

- Chóp cụt có hai đáy diện tích $S_1,S_2$ và chiều cao $h$:

  $$

  V_{\text{cụt}}=\frac h3\left(S_1+S_2+\sqrt{S_1S_2}\right).

  $$

- Nếu hai khối đa diện đồng dạng với tỉ số độ dài $k$ thì:

  $$

  \frac{S_1}{S_2}=k^2,\qquad

  \frac{V_1}{V_2}=k^3.

  $$

  

### 5.3. Quan hệ vuông góc, song song

  

- Đường thẳng $d$ vuông góc mặt phẳng $(P)$ nếu $d$ vuông góc với hai đường thẳng cắt nhau nằm trong $(P)$.

- Hai mặt phẳng vuông góc nếu một mặt phẳng chứa một đường thẳng vuông góc với mặt phẳng kia.

- Đường thẳng song song mặt phẳng nếu song song với một đường thẳng nằm trong mặt phẳng đó.

- Hai mặt phẳng song song nếu chúng có hai cặp đường thẳng cắt nhau tương ứng song song.

  

### 5.4. Góc và khoảng cách trong không gian

  

- Góc giữa hai đường thẳng:

  $$

  \cos\varphi=\frac{|\vec u_1\cdot\vec u_2|}{|\vec u_1||\vec u_2|}.

  $$

- Góc giữa đường thẳng và mặt phẳng:

  $$

  \sin\varphi=\frac{|\vec u\cdot\vec n|}{|\vec u||\vec n|},

  $$

  trong đó $\vec u$ là vector chỉ phương đường thẳng, $\vec n$ là vector pháp tuyến mặt phẳng.

- Góc giữa hai mặt phẳng:

  $$

  \cos\varphi=\frac{|\vec n_1\cdot\vec n_2|}{|\vec n_1||\vec n_2|}.

  $$

- Các công thức khoảng cách và hình chiếu được gom ở mục 10.

  

## 6. Tọa độ không gian $Oxyz$

  

### 6.1. Điểm, vector, mặt cầu

  

- Khoảng cách:

  $$

  AB=\sqrt{(x_B-x_A)^2+(y_B-y_A)^2+(z_B-z_A)^2}.

  $$

  

- Mặt cầu tâm $I(a,b,c)$, bán kính $R$:

  $$

  (x-a)^2+(y-b)^2+(z-c)^2=R^2.

  $$

  

### 6.2. Đường thẳng trong không gian

  

Qua $M_0(x_0,y_0,z_0)$, có vector chỉ phương $\vec u=(a,b,c)$:

  

- Dạng tham số:

  $$

  \begin{cases}

  x=x_0+at,\\

  y=y_0+bt,\\

  z=z_0+ct.

  \end{cases}

  $$

- Dạng chính tắc:

  $$

  \frac{x-x_0}{a}=\frac{y-y_0}{b}=\frac{z-z_0}{c}.

  $$

  

### 6.3. Mặt phẳng

  

- Dạng tổng quát:

  $$

  ax+by+cz+d=0,\qquad \vec n=(a,b,c).

  $$

- Qua điểm $M_0(x_0,y_0,z_0)$, có pháp tuyến $\vec n=(a,b,c)$:

  $$

  a(x-x_0)+b(y-y_0)+c(z-z_0)=0.

  $$

- Qua ba điểm $A,B,C$:

  $$

  \vec n=\vec{AB}\times\vec{AC}.

  $$

- Dạng tham số qua điểm $M_0$, có hai vector chỉ phương không cùng phương $\vec u,\vec v$:

  $$

  \begin{cases}

  x=x_0+u_1s+v_1t,\\

  y=y_0+u_2s+v_2t,\\

  z=z_0+u_3s+v_3t.

  \end{cases}

  $$

  

### 6.6. Điều kiện thẳng hàng, đồng phẳng

  

- Ba điểm $A,B,C$ thẳng hàng:

  $$

  \vec{AB}\times\vec{AC}=\vec0.

  $$

  Cũng có thể dùng dạng affine: tồn tại $p,q\in\mathbb R$ sao cho

  $$

  \vec{IA}=p\vec{IB}+q\vec{IC},\qquad p+q=1.

  $$

- Bốn điểm $A,B,C,D$ đồng phẳng:

  $$

  \vec{AB}\cdot(\vec{AC}\times\vec{AD})=0.

  $$

- Một điểm $M$ thuộc mặt phẳng $(ABC)$:

  $$

  \vec{AB}\cdot(\vec{AC}\times\vec{AM})=0.

  $$

  

### 6.7. Tâm đặc biệt trong tam giác không gian

  

Với tam giác $ABC$ đã biết tọa độ:

  

- Tâm tỉ cự của hệ $A,B,C$ ứng với các hệ số $\alpha,\beta,\gamma$ và $\alpha+\beta+\gamma\ne0$:

  $$

  I=\frac{\alpha A+\beta B+\gamma C}{\alpha+\beta+\gamma}.

  $$

  

  Tương đương:

  $$

  \alpha\vec{IA}+\beta\vec{IB}+\gamma\vec{IC}=\vec0.

  $$

  Với mọi điểm $M$:

  $$

  \alpha\vec{MA}+\beta\vec{MB}+\gamma\vec{MC}

  =(\alpha+\beta+\gamma)\vec{MI}.

  $$

  $$

  \alpha MA^2+\beta MB^2+\gamma MC^2

  =\alpha IA^2+\beta IB^2+\gamma IC^2+(\alpha+\beta+\gamma)MI^2.

  $$

- Tâm đường tròn nội tiếp, với $a=BC,\ b=CA,\ c=AB$:

  $$

  J=\frac{aA+bB+cC}{a+b+c}.

  $$

- Chân phân giác trong từ $A$ xuống $BC$:

  $$

  D=\frac{AC\cdot B+AB\cdot C}{AB+AC}.

  $$

  Nếu đặt $k=-\dfrac{AB}{AC}$ thì:

  $$

  D=\frac{B-kC}{1-k}.

  $$

- Trực tâm $H$ của tam giác $ABC$ trong không gian được tìm từ:

  $$

  \begin{cases}

  \vec{BH}\cdot\vec{AC}=0,\\

  \vec{CH}\cdot\vec{AB}=0,\\

  \vec{AB}\cdot(\vec{AC}\times\vec{AH})=0.

  \end{cases}

  $$

- Tâm đường tròn ngoại tiếp $I$ của tam giác $ABC$ được tìm từ giao điểm của 2 đường trung trực bất kì, công thức đường trung trực ở mục dưới

  

- Bán kính đường tròn ngoại tiếp tam giác $ABC$:

  $$

  R=\frac{AB\cdot BC\cdot CA}{2|\vec{AB}\times\vec{AC}|}.

  $$

  

### 6.8. Một số mặt phẳng đặc biệt

  

- Mặt phẳng chứa trục $Ox$:

  $$

  by+cz=0,\qquad b^2+c^2>0.

  $$

- Mặt phẳng chứa trục $Oy$:

  $$

  ax+cz=0,\qquad a^2+c^2>0.

  $$

- Mặt phẳng chứa trục $Oz$:

  $$

  ax+by=0,\qquad a^2+b^2>0.

  $$

- Mặt phẳng chắn ba trục tại $A(a,0,0)$, $B(0,b,0)$, $C(0,0,c)$ với $abc\ne0$:

  $$

  \frac xa+\frac yb+\frac zc=1.

  $$

- Nếu mặt phẳng chắn có tam giác chắn trục nhận $G(x_G,y_G,z_G)$ làm trọng tâm:

  $$

  \frac{x}{3x_G}+\frac{y}{3y_G}+\frac{z}{3z_G}=1.

  $$

- Nếu mặt phẳng chắn có tam giác chắn trục nhận $H(x_H,y_H,z_H)$ làm trực tâm:

  $$

  x_Hx+y_Hy+z_Hz=x_H^2+y_H^2+z_H^2.

  $$

- Nếu mặt phẳng chắn có tam giác chắn trục nhận $I(x_I,y_I,z_I)$ làm tâm ngoại tiếp, các giao điểm $A(a,0,0)$, $B(0,b,0)$, $C(0,0,c)$ thỏa:

  $$

  IA^2=IB^2,\qquad IA^2=IC^2,\qquad

  \frac{x_I}{a}+\frac{y_I}{b}+\frac{z_I}{c}=1.

  $$

  

### 6.9. Giao điểm và vị trí tương đối

  

- Điểm thuộc đường thẳng

  $$

  d:\begin{cases}

  x=x_0+at,\\

  y=y_0+bt,\\

  z=z_0+ct

  \end{cases}

  $$

  có dạng:

  $$

  M(x_0+at_0,\ y_0+bt_0,\ z_0+ct_0),\qquad t_0\in\mathbb R.

  $$

- Điểm thuộc mặt phẳng $(P):ax+by+cz+d=0$: chọn hai tọa độ tùy ý rồi thay vào:

  $$

  ax_M+by_M+cz_M+d=0

  $$

  để tìm tọa độ còn lại.

- Điểm thuộc giao tuyến của hai mặt phẳng:

  $$

  \begin{cases}

  ax+by+cz+d=0,\\

  a'x+b'y+c'z+d'=0

  \end{cases}

  $$

  được tìm bằng cách chọn một tọa độ tùy ý rồi giải hệ hai phương trình còn lại.

- Giao tuyến của hai mặt phẳng $(P)$ và $(Q)$ có pháp tuyến $\vec n_P,\vec n_Q$:

  $$

  \vec u=\vec n_P\times\vec n_Q.

  $$

  Lấy thêm một điểm chung bằng cách giải hệ hai phương trình mặt phẳng.

- Giao điểm của đường thẳng

  $$

  d:\begin{cases}

  x=x_0+at,\\

  y=y_0+bt,\\

  z=z_0+ct

  \end{cases}

  $$

  và mặt phẳng $(P):Ax+By+Cz+D=0$ được tìm từ:

  $$

  A(x_0+at)+B(y_0+bt)+C(z_0+ct)+D=0.

  $$

- Giao điểm của đường thẳng $d$ ở trên và mặt cầu $(S):(x-A)^2+(y-B)^2+(z-C)^2=R^2$ được tìm từ:

  $$

  (x_0+at-A)^2+(y_0+bt-B)^2+(z_0+ct-C)^2=R^2.

  $$

- Hai mặt phẳng

  $$

  (P):ax+by+cz+d=0,\qquad

  (Q):a'x+b'y+c'z+d'=0

  $$

  song song hoặc trùng nhau khi:

  $$

  (a,b,c)=k(a',b',c').

  $$

  Nếu thêm $d=kd'$ thì trùng nhau, nếu $d\ne kd'$ thì song song phân biệt.

- Hai mặt phẳng cắt nhau khi:

  $$

  (a,b,c)\not\parallel(a',b',c').

  $$

- Với hai đường thẳng $d,d'$ lần lượt đi qua $M,M'$ và có vector chỉ phương $\vec u,\vec v$, đặt:

  $$

  \vec n=\vec u\times\vec v.

  $$

  Nếu $\vec n=\vec0$ thì:

  $$

  \vec{MM'}\times\vec u=\vec0 \Leftrightarrow d\equiv d',

  $$

  $$

  \vec{MM'}\times\vec u\ne\vec0 \Leftrightarrow d\parallel d'.

  $$

  Nếu $\vec n\ne\vec0$ thì:

  $$

  \vec{MM'}\cdot\vec n=0 \Leftrightarrow d,d' \text{ cắt nhau},

  $$

  $$

  \vec{MM'}\cdot\vec n\ne0 \Leftrightarrow d,d' \text{ chéo nhau}.

  $$

  

### 6.10. Hình chiếu, đối xứng và đường vuông góc chung

  

- Hình chiếu của $M(x_M,y_M,z_M)$ lên mặt phẳng $(P):ax+by+cz+d=0$:

  $$

  H(x_M+at,\ y_M+bt,\ z_M+ct),

  \qquad

  t=-\frac{ax_M+by_M+cz_M+d}{a^2+b^2+c^2}.

  $$

  Tương đương:

  $$

  H=M-\frac{ax_M+by_M+cz_M+d}{a^2+b^2+c^2}(a,b,c).

  $$

- Hình chiếu của $M$ lên đường thẳng $d$ qua $A$, có vector chỉ phương $\vec u$:

  $$

  H=A+\frac{\vec{AM}\cdot\vec u}{|\vec u|^2}\vec u.

  $$

- Điểm đối xứng của $M$ qua đường thẳng $d$:

  $$

  M'=2H-M.

  $$

- Hình chiếu của vector $\vec u$ lên mặt phẳng có pháp tuyến $\vec n$:

  $$

  \vec u_{(P)}=\vec u-\frac{\vec u\cdot\vec n}{|\vec n|^2}\vec n.

  $$

- Hình chiếu của đường thẳng $d$ lên mặt phẳng $(P)$ có thể lấy vector chỉ phương:

  $$

  \vec u'=\vec n\times(\vec u\times\vec n).

  $$

- Đường thẳng đối xứng của $d$ qua mặt phẳng $(P)$: lấy hai điểm phân biệt $A,B\in d$, dựng hai điểm đối xứng $A',B'$ qua $(P)$, rồi viết đường thẳng $A'B'$.

  Cách dựng theo vị trí:

  

  - Nếu $d\parallel(P)$, lấy $I\in d$, dựng $I'$ đối xứng với $I$ qua $(P)$; đường thẳng đối xứng $d'$ đi qua $I'$ và có vector chỉ phương $\vec u_d$.

  - Nếu $d\cap(P)=\{M\}$, lấy $N\in d$, $N\ne M$, dựng $N'$ đối xứng với $N$ qua $(P)$; khi đó $d'$ đi qua $N'$ và có vector chỉ phương $\vec{MN'}$.

- Đường vuông góc chung của hai đường thẳng chéo nhau có vector chỉ phương:

  $$

  \vec u_\Delta=\vec u\times\vec v.

  $$

  Cách dựng nhanh: viết mặt phẳng $(P)$ chứa $d$ và chứa đường vuông góc chung $\Delta$, với

  $$

  \vec n_P=\vec u_d\times\vec u_\Delta.

  $$

  Nếu $d'\cap(P)=\{M\}$ thì $\Delta$ đi qua $M$ và có vector chỉ phương $\vec u_\Delta$.

  

### 6.11. Mặt cầu mở rộng

  

- Dạng tổng quát của mặt cầu:

  $$

  x^2+y^2+z^2-2x-2by-2cz+d=0.

  $$

- Tâm và bán kính:

  $$

  I\left(a,b,c\right),\qquad

  R=\sqrt{a^2+b^2+c^2-d}.

  $$

- Mặt cầu tâm $I$ tiếp xúc mặt phẳng $(P)$:

  $$

  R=d(I,(P)).

  $$

- Mặt cầu đi qua bốn điểm $A,B,C,D$: đặt dạng tổng quát rồi thay tọa độ bốn điểm để tìm $a,b,c,d$.

- Mặt cầu đi qua ba điểm $A,B,C$ và có tâm $I$ thuộc mặt phẳng $(P)$:

  $$

  \begin{cases}

  IA^2=IB^2,\\

  IA^2=IC^2,\\

  I\in(P).

  \end{cases}

  $$

- Mặt cầu đi qua hai điểm $A,B$ và có tâm $I$ thuộc đường thẳng $d$: tâm $I$ nằm trên mặt phẳng trung trực của $AB$:

  $$

  IA^2=IB^2.

  $$

- Mặt cầu đi qua điểm $A$, có tâm $I$ thuộc đường thẳng $d$ và tiếp xúc với mặt phẳng $(P)$:

  $$

  IA=d(I,(P)).

  $$

- Mặt cầu tâm $I$ bị mặt phẳng $(P)$ cắt theo đường tròn bán kính $r$:

  nếu đề cho diện tích $S$ hoặc chu vi $C$ của đường tròn giao tuyến thì:

  $$

  S=\pi r^2,\qquad C=2\pi r.

  $$

  Khi đó:

  
  

  $$

  R^2=r^2+d^2(I,(P)).

  $$

- Mặt cầu tâm $I$ bị đường thẳng $d$ cắt theo dây cung dài $\ell$:

  $$

  R^2=d^2(I,d)+\left(\frac{\ell}{2}\right)^2.

  $$

  

### 6.12. Mẫu dựng phương trình nhanh trong $Oxyz$

  

- Mặt phẳng chứa đường thẳng $d$ qua $M$, chỉ phương $\vec u$, và đi qua điểm $A\notin d$:

  $$

  \vec n=\vec u\times\vec{MA}.

  $$

- Mặt phẳng chứa đường thẳng $d$ và vuông góc với mặt phẳng $(Q)$ có pháp tuyến $\vec n_Q$:

  $$

  \vec n_P=\vec n_Q\times\vec u.

  $$

- Đường thẳng qua $A$, cắt và vuông góc với đường thẳng $d$ qua $M$, chỉ phương $\vec u$:

  $$

  \vec u_\Delta=\vec u\times(\vec u\times\vec{MA}).

  $$

- Đường thẳng qua $A$, cắt đường thẳng $d$ qua $M$, chỉ phương $\vec u$, và vuông góc với đường thẳng $d'$ có chỉ phương $\vec u'$:

  $$

  \vec u_\Delta=\vec u'\times(\vec u\times\vec{MA}).

  $$

- Đường thẳng nằm trong mặt phẳng $(P)$, cắt cả hai đường thẳng $d,d'$:

  lấy

  $$

  d\cap(P)=\{A\},\qquad d'\cap(P)=\{B\},

  $$

  khi đó đường thẳng cần tìm đi qua $A$ và có vector chỉ phương:

  $$

  \vec u=\vec{AB}.

  $$

- Đường thẳng qua điểm $A$, cắt cả hai đường thẳng $d,d'$:

  có thể gọi $B(t)\in d$, $B'(t')\in d'$ rồi dùng điều kiện $A,B,B'$ thẳng hàng. Hoặc viết mặt phẳng $(P)$ chứa $A$ và $d$:

  $$

  \vec n_P=\vec u_d\times\vec{AM}.

  $$

  Lấy $d'\cap(P)=\{B\}$, khi đó đường thẳng cần tìm đi qua $A$ và có vector chỉ phương:

  $$

  \vec u_\Delta=\vec{AB}.

  $$

- Đường thẳng song song với $\Delta$, cắt cả hai đường thẳng $d,d'$:

  có thể gọi $A(a)\in d$, $B(b)\in d'$ rồi dùng $AB\parallel\Delta$. Hoặc viết mặt phẳng $(P)$ chứa $d$ và song song với $\Delta$:

  $$

  \vec n_P=\vec u_d\times\vec u_\Delta.

  $$

  Lấy $d'\cap(P)=\{B\}$, khi đó đường thẳng cần tìm đi qua $B$ và có vector chỉ phương:

  $$

  \vec u=\vec u_\Delta.

  $$

- Đường trung tuyến từ $A$ của tam giác $ABC$: đi qua $A$ và trung điểm $M_{BC}$ của $BC$.

- Đường cao từ $A$ của tam giác $ABC$ có thể lấy vector chỉ phương:

  $$

  \vec u_h=\vec{BC}\times(\vec{BC}\times\vec{BA}).

  $$

- Đường trung trực của $BC$ trong mặt phẳng $(ABC)$: đi qua trung điểm $M_{BC}$ và có vector chỉ phương:

  $$

  \vec u=\vec u_h

  $$

- Phân giác trong và ngoài góc $A$ của tam giác $ABC$ có vector chỉ phương:

  $$

  \vec u_{\text{trong}}=\frac{\vec{AB}}{|\vec{AB}|}+\frac{\vec{AC}}{|\vec{AC}|},\qquad

  \vec u_{\text{ngoài}}=\frac{\vec{AB}}{|\vec{AB}|}-\frac{\vec{AC}}{|\vec{AC}|}.

  $$

  

### 6.13. Một số dạng cực trị hình học tọa độ

  

- Mặt phẳng đi qua $A$ và cách điểm $M$ một khoảng lớn nhất:

  $$

  \vec n=\vec{AM}.

  $$

- Đường thẳng nằm trong mặt phẳng $(P)$, song song với $d$ và cách $d$ một khoảng nhỏ nhất, với $d\parallel(P)$:

  lấy $A\in d$, chiếu $A$ xuống $(P)$ được $A'$. Đường thẳng cần tìm đi qua $A'$ và có vector chỉ phương:

  $$

  \vec u=\vec u_d.

  $$

- Đường thẳng nằm trong $(P)$, đi qua $A\in(P)$ và cách điểm $M$ một khoảng lớn nhất:

  $$

  \vec u=\vec n_P\times\vec{AM}.

  $$

- Đường thẳng nằm trong $(P)$, đi qua $A\in(P)$ và cách điểm $M$ một khoảng nhỏ nhất: lấy $H$ là hình chiếu của $M$ lên $(P)$, khi đó:

  $$

  \vec u=\vec{AH}.

  $$

  Cũng có thể dùng trực tiếp:

  $$

  \vec u=\vec n_P\times(\vec n_P\times\vec{AM}).

  $$

- Mặt phẳng chứa đường thẳng $d$ qua $A$, chỉ phương $\vec u_d$, và cách điểm $M$ một khoảng lớn nhất:

  $$

  \vec n=(\vec u_d\times\vec{AM})\times\vec u_d.

  $$

- Đường thẳng nằm trong $(P)$, qua $A$, và cách đường thẳng $d$ một khoảng lớn nhất, với $d$ cắt $(P)$:

  lấy $B\in d$ bất kì, khi đó có thể lấy vector chỉ phương:

  $$

  \vec u_\Delta

  =\vec n_P\times\left(\vec u_d\times(\vec u_d\times\vec{AB})\right).

  $$

- Mặt phẳng chứa đường thẳng $\Delta$, tạo với đường thẳng $d$ một góc lớn nhất:

  lấy $A\in\Delta$, khi đó mặt phẳng cần tìm đi qua $A$ và có pháp tuyến:

  $$

  \vec n_P=\vec u_\Delta\times(\vec u_\Delta\times\vec u_d).

  $$

- Đường thẳng nằm trong $(P)$ và tạo với đường thẳng $d$ một góc nhỏ nhất có vector chỉ phương bằng hình chiếu của $\vec u_d$ lên $(P)$:

  $$

  \vec u=\vec n_P\times(\vec n_P\times\vec u_d).

  $$

  

## 7. Kĩ thuật tham số hóa

  

### 7.1. Tham số hóa đoạn thẳng

  

Với $A,B$:

  

$$

M=A+t(B-A),\qquad 0\le t\le1.

$$

  

- Nếu $t=0$, $M=A$.

- Nếu $t=1$, $M=B$.

- Nếu $t=\frac12$, $M$ là trung điểm.

- Nếu không giới hạn $t$, ta được cả đường thẳng $AB$.

  

### 7.2. Tham số hóa đường thẳng

  

Qua $A$, chỉ phương $\vec u$:

  

$$

M=A+t\vec u,\qquad t\in\mathbb R.

$$

  

Dùng khi điểm $M$ chạy trên một đường thẳng và cần biến điều kiện hình học thành phương trình theo $t$.

  

### 7.3. Tham số hóa đường tròn

  

Đường tròn tâm $I(a,b)$, bán kính $R$:

  

$$

\begin{cases}

x=a+R\cos t,\\

y=b+R\sin t.

\end{cases}

$$

  

Với nửa đường tròn hoặc cung tròn, giới hạn miền của $t$.

  

### 7.4. Tham số hóa mặt phẳng

  

Mặt phẳng qua $A$, có hai vector chỉ phương $\vec u,\vec v$:

  

$$

M=A+s\vec u+t\vec v,\qquad s,t\in\mathbb R.

$$

  

Nếu $M$ nằm trong tam giác $ABC$:

  

$$

M=A+s(B-A)+t(C-A),\qquad s\ge0,\quad t\ge0,\quad s+t\le1.

$$

  

### 7.5. Tham số hóa bằng tỉ số

  

Nếu $M\in AB$ và:

  

$$

\frac{AM}{MB}=\frac mn,

$$

  

thì:

  

$$

M=\frac{nA+mB}{m+n}.

$$

  

Nếu dùng tham số:

  

$$

M=A+\frac{m}{m+n}(B-A).

$$

  

### 7.6. Tham số hóa điều kiện cực trị

  

Các dạng hay gặp:

  

- $M$ trên đường thẳng: đặt $M=A+t\vec u$, đưa biểu thức cần tối ưu về hàm một biến $f(t)$.

- $M$ trên đoạn: đặt $t\in[0,1]$, xét thêm hai đầu mút.

- $M$ trên đường tròn: đặt $M=I+R(\cos t,\sin t)$, dùng lượng giác hoặc tích vô hướng.

- $M$ trên mặt phẳng: đặt $M=A+s\vec u+t\vec v$, giải hệ theo $s,t$.

  

## 8. Kĩ thuật tọa độ hóa

  

### 8.1. Nguyên tắc chọn hệ tọa độ

  

- Đặt gốc tọa độ tại điểm đặc biệt: đỉnh vuông, tâm, trọng tâm, trung điểm.

- Đặt trục theo cạnh, đường cao, đường trung tuyến, đường đối xứng.

- Ưu tiên biến ít nhất: cạnh đã biết đặt trùng trục để nhiều tọa độ bằng $0$.

- Với hình vuông, chữ nhật, hộp chữ nhật: đặt các cạnh song song trục.

- Với tam giác vuông: đặt đỉnh vuông tại $O$, hai cạnh góc vuông theo $Ox,Oy$.

- Với hình chóp có đáy phẳng: đặt đáy trong mặt phẳng $Oxy$, chiều cao theo $Oz$.

  

### 8.2. Mẫu tọa độ hóa thường dùng

  

- Tam giác vuông tại $A$:

  $$

  A(0,0),\qquad B(b,0),\qquad C(0,c).

  $$

- Tam giác cân tại $A$, đáy $BC$:

  $$

  B(-a,0),\qquad C(a,0),\qquad A(0,h).

  $$

- Tam giác đều cạnh $a$:

  $$

  A(0,0),\qquad B(a,0),\qquad C\left(\frac a2,\frac{a\sqrt3}{2}\right).

  $$

- Hình chữ nhật:

  $$

  A(0,0),\quad B(a,0),\quad C(a,b),\quad D(0,b).

  $$

- Hình hộp chữ nhật:

  $$

  A(0,0,0),\quad B(a,0,0),\quad D(0,b,0),\quad A'(0,0,c).

  $$

- Chóp có đáy $ABC$ nằm trên $Oxy$, chân đường cao tại $H(x_H,y_H,0)$:

  $$

  S(x_H,y_H,h).

  $$

  

### 8.3. Mô hình xác định chân đường cao của hình chóp

  

Với hình chóp $S.ABC$, gọi $I$ là hình chiếu vuông góc của $S$ lên mặt phẳng đáy $(ABC)$:

  

$$

SI\perp(ABC),\qquad S=I+h\vec n

$$

  

trong đó $\vec n$ là vector pháp tuyến đơn vị của $(ABC)$.

  

Nguyên lí hay dùng:

  

- Với mọi điểm $P$ thuộc đáy:

  $$

  SP^2=SI^2+IP^2.

  $$

  Vì vậy:

  $$

  SP=SQ\Leftrightarrow IP=IQ.

  $$

- Với mọi đường thẳng $d$ nằm trong đáy:

  $$

  d^2(S,d)=SI^2+d^2(I,d).

  $$

  Vì vậy:

  $$

  d(S,d_1)=d(S,d_2)\Leftrightarrow d(I,d_1)=d(I,d_2).

  $$

- Góc giữa cạnh bên $SP$ và đáy:

  $$

  \sin\angle(SP,(ABC))=\frac{SI}{SP}.

  $$

- Góc nhị diện giữa mặt bên $(SAB)$ và đáy theo cạnh $AB$:

  $$

  \tan\varphi_{AB}=\frac{SI}{d(I,AB)}.

  $$

  

Các mô hình nhận biết nhanh:

  

- Nếu $SA=SB=SC$ thì:

  $$

  IA=IB=IC,

  $$

  nên $I$ là tâm đường tròn ngoại tiếp tam giác $ABC$.

- Nếu các cạnh bên $SA,SB,SC$ tạo với đáy các góc bằng nhau thì $SA=SB=SC$, nên $I$ là tâm ngoại tiếp của $ABC$.

- Nếu $d(S,AB)=d(S,BC)=d(S,CA)$ thì:

  $$

  d(I,AB)=d(I,BC)=d(I,CA),

  $$

  nên $I$ là tâm nội tiếp hoặc tâm bàng tiếp của $ABC$. Nếu $I$ nằm trong tam giác thì $I$ là tâm nội tiếp.

- Nếu các mặt bên $(SAB),(SBC),(SCA)$ tạo với đáy các góc nhị diện bằng nhau thì:

  $$

  d(I,AB)=d(I,BC)=d(I,CA),

  $$

  nên $I$ là tâm nội tiếp hoặc tâm bàng tiếp.

- Nếu

  $$

  \frac{S_{SAB}}{AB}=\frac{S_{SBC}}{BC}=\frac{S_{SCA}}{CA},

  $$

  thì các khoảng cách từ $S$ đến $AB,BC,CA$ bằng nhau, nên $I$ là tâm nội tiếp hoặc tâm bàng tiếp.

- Nếu $I$ chia tam giác đáy thành ba tam giác có diện tích bằng nhau:

  $$

  S_{IBC}=S_{ICA}=S_{IAB},

  $$

  thì $I$ là trọng tâm của $ABC$.

- Tương đương, nếu các khối chóp nhỏ có cùng chiều cao từ $S$ xuống đáy và:

  $$

  V_{S.IBC}=V_{S.ICA}=V_{S.IAB},

  $$

  thì $I$ là trọng tâm của $ABC$.

- Nếu

  $$

  \vec{SA}+\vec{SB}+\vec{SC}\perp(ABC),

  $$

  thì phần chiếu của tổng vector trên đáy bằng $\vec0$, do đó $I$ là trọng tâm:

  $$

  I=\frac{A+B+C}{3}.

  $$

- Nếu $SA\perp BC$ và $SB\perp CA$ thì:

  $$

  AI\perp BC,\qquad BI\perp CA,

  $$

  nên $I$ là trực tâm của $ABC$. Chỉ cần hai điều kiện; điều kiện thứ ba tự suy ra.

- Nếu $SA,SB,SC$ đôi một vuông góc thì $I$ là trực tâm của $ABC$.

- Nếu $SA=SB$ thì:

  $$

  IA=IB,

  $$

  nên $I$ nằm trên đường trung trực của $AB$.

- Nếu $d(S,AB)=d(S,AC)$ thì:

  $$

  d(I,AB)=d(I,AC),

  $$

  nên $I$ nằm trên phân giác trong hoặc ngoài của góc $A$.

- Nếu $SA=SB$ và $I\in AB$ thì $I$ là trung điểm của $AB$.

- Nếu $d(S,AB)=d(S,AC)$ và $I\in BC$ thì $I$ là chân phân giác trong hoặc ngoài từ $A$ xuống $BC$.

- Nếu $SI\perp(ABC)$ và đề cho trực tiếp $I$ nằm trên trung tuyến, phân giác, đường cao, trung trực hoặc một đường đặc biệt của đáy, hãy kết hợp với một điều kiện thứ hai để xác định $I$ là giao điểm của hai đường trong tam giác đáy.

  

Mô hình tổng quát bằng trọng số:

  

- Nếu biết tỉ lệ diện tích:

  $$

  S_{IBC}:S_{ICA}:S_{IAB}=\lambda_A:\lambda_B:\lambda_C,

  $$

  thì:

  $$

  I=\frac{\lambda_AA+\lambda_BB+\lambda_CC}{\lambda_A+\lambda_B+\lambda_C}.

  $$

- Nếu biết khoảng cách có hướng từ $I$ đến các cạnh:

  $$

  d(I,BC):d(I,CA):d(I,AB)=x:y:z,

  $$

  thì tọa độ barycentric của $I$ theo tam giác $ABC$ tỉ lệ:

  $$

  I\sim ax:by:cz,

  $$

  trong đó $a=BC,\ b=CA,\ c=AB$.

  

### 8.4. Quy trình tọa độ hóa

  

1. Chọn hệ trục làm nhiều tọa độ bằng $0$.

2. Gán tọa độ các điểm cố định.

3. Biểu diễn điểm động bằng tham số.

4. Chuyển điều kiện hình học thành phương trình tọa độ hoặc vector.

5. Giải phương trình, rồi kiểm tra miền tham số.

  

## 9. Kĩ thuật tách vector

  

### 9.1. Tách theo hai vector cơ sở

  

Nếu $\vec u,\vec v$ không cùng phương, mọi vector trong mặt phẳng của chúng có thể viết:

  

$$

\vec x=\alpha\vec u+\beta\vec v.

$$

$$

|\vec x|=(\alpha\vec u)^2+(\beta\vec v)^2+  + 2(\alpha\vec u\cdot\beta\vec v).

$$

Dùng khi muốn biến quan hệ hình học thành hệ phương trình theo $\alpha,\beta$.

  

### 9.2. Tách theo ba vector cơ sở trong không gian

  

Nếu $\vec u,\vec v,\vec w$ không đồng phẳng:

  

$$

\vec x=\alpha\vec u+\beta\vec v+\gamma\vec w.

$$

  

$$

|\vec x|=(\alpha\vec u)^2+(\beta\vec v)^2+ (\gamma\vec w)^2 + 2(\alpha\vec u\cdot\beta\vec v)+2(\alpha\vec u\cdot\gamma\vec w)+2(\beta\vec v\cdot\gamma w).

$$

  

Các hệ số $\alpha,\beta,\gamma$ thường tìm bằng cách so sánh tọa độ hoặc giải hệ tuyến tính.

  

### 9.3. Tách vector theo phương song song và vuông góc

  

Với vector $\vec a$ và phương $\vec u$:

  

$$

\vec a=\vec a_{\parallel}+\vec a_{\perp}.

$$

  

Trong đó:

  

$$

\vec a_{\parallel}=\frac{\vec a\cdot\vec u}{|\vec u|^2}\vec u,\qquad

\vec a_{\perp}=\vec a-\vec a_{\parallel}.

$$

  

Dùng nhiều trong bài toán khoảng cách, hình chiếu, cực trị.

  

### 9.4. Tách vector trong tam giác

  

Với tam giác $ABC$, chọn gốc tại $A$:

  

$$

\vec{AM}=x\vec{AB}+y\vec{AC}.

$$

  

- $M$ nằm trên đường thẳng $BC$:

  $$

  x+y=1.

  $$

- $M$ nằm trong tam giác $ABC$:

  $$

  x\ge0,\qquad y\ge0,\qquad x+y\le1.

  $$

- $M$ là trung điểm $BC$:

  $$

  \vec{AM}=\frac12\vec{AB}+\frac12\vec{AC}.

  $$

- $G$ là trọng tâm:

  $$

  \vec{AG}=\frac13\vec{AB}+\frac13\vec{AC}.

  $$

  

### 9.5. Tách bằng trọng số affine

  

Điểm $M$ được biểu diễn bởi các điểm $A_1,A_2,\dots,A_n$:

  

$$

M=\lambda_1A_1+\lambda_2A_2+\cdots+\lambda_nA_n,

$$

  

với:

  

$$

\lambda_1+\lambda_2+\cdots+\lambda_n=1.

$$

  

Nếu mọi $\lambda_i\ge0$, điểm $M$ nằm trong bao lồi của các điểm $A_i$.

  

## 10. Công thức khoảng cách thường dùng

  

### 10.1. Điểm đến đường thẳng

  

Đường thẳng $d$ qua $A$, có vector chỉ phương $\vec u$, điểm $M$:

  

$$

d(M,d)=\frac{|\,\vec{AM}\times\vec u\,|}{|\vec u|}.

$$

  

### 10.2. Điểm đến mặt phẳng

  

Mặt phẳng $(P):ax+by+cz+d=0$, điểm $M(x_0,y_0,z_0)$:

  

$$

d(M,(P))=\frac{|ax_0+by_0+cz_0+d|}{\sqrt{a^2+b^2+c^2}}.

$$

  

Nếu $H$ là hình chiếu của $M$ lên $(P)$ thì:

  

$$

H=M-\frac{ax_0+by_0+cz_0+d}{a^2+b^2+c^2}(a,b,c).

$$

  

### 10.3. Hai mặt phẳng song song

  

Nếu

  

$$

(P):ax+by+cz+d_1=0,\qquad

(Q):ax+by+cz+d_2=0

$$

  

thì:

  

$$

d((P),(Q))=\frac{|d_1-d_2|}{\sqrt{a^2+b^2+c^2}}.

$$

  

### 10.4. Hai đường thẳng chéo nhau

  

Cho $d_1$ qua $A$, có vector chỉ phương $\vec u$; $d_2$ qua $B$, có vector chỉ phương $\vec v$. Khi $\vec u\times\vec v\ne\vec0$:

  

$$

d(d_1,d_2)=\frac{|\,\vec{AB}\cdot(\vec u\times\vec v)\,|}{|\vec u\times\vec v|}.

$$

  

Nếu $d_1=AB$, $d_2=CD$ trong tứ diện $ABCD$ thì:

  

$$

d(AB,CD)=\frac{6V_{ABCD}}{|\,\vec{AB}\times\vec{CD}\,|}.

$$

  

Công thức DKH cho mô hình đặc biệt:

  

- Xét hai đường chéo nhau $AB$ và $CD$.

- Lấy $M$ là hình chiếu của $B$ lên $CD$, nên:

  $$

  a=d(B,CD)=BM.

  $$

- Lấy $H$ là hình chiếu của $A$ lên mặt phẳng chứa $B$ và $CD$, kẻ BH cắt CD tại $K$.

- Đặt:

  $$

  h=AH,\qquad k=\frac{BH}{BK}.

  $$

  

Khi đó:

  

$$

\frac1{d^2(AB,CD)}=\frac1{a^2}+\frac{k^2}{h^2}.

$$

  

Tương đương:

  

$$

d(AB,CD)=\frac{ah}{\sqrt{h^2+k^2a^2}}.

$$

  
  

## 11. Kĩ thuật giải nhanh theo dạng bài

  

### 11.1. Chứng minh vuông góc

  

Các hướng thường dùng:

  

- Chứng minh tích vô hướng bằng $0$.

- Chứng minh đường thẳng vuông góc với hai đường cắt nhau trong mặt phẳng.

- Dùng định lí Pythagore đảo.

- Dùng vector pháp tuyến hoặc vector chỉ phương.

  

### 11.2. Chứng minh song song

  

Các hướng thường dùng:

  

- Chứng minh hai vector cùng phương.

- Chứng minh hai đường có cùng vector pháp tuyến hoặc chỉ phương.

- Dùng định lí Thales, đường trung bình.

- Trong không gian, chứng minh đường thẳng song song với một đường nằm trong mặt phẳng.

  

### 11.3. Tính góc

  

Các hướng thường dùng:

  

- Đưa về góc giữa hai vector:

  $$

  \cos\theta=\frac{\vec u\cdot\vec v}{|\vec u||\vec v|}.

  $$

- Đường thẳng và mặt phẳng:

  $$

  \sin\varphi=\frac{|\vec u\cdot\vec n|}{|\vec u||\vec n|}.

  $$

- Hai mặt phẳng:

  $$

  \cos\varphi=\frac{|\vec n_1\cdot\vec n_2|}{|\vec n_1||\vec n_2|}.

  $$

  

### 11.4. Tính khoảng cách

  

Các hướng thường dùng:

  

- Điểm đến đường thẳng: dùng hình chiếu hoặc công thức vector ở mục 10.1.

- Điểm đến mặt phẳng: dùng công thức pháp tuyến ở mục 10.2.

- Hai mặt phẳng song song: đưa về khoảng cách từ một điểm trên mặt này đến mặt kia.

- Hai đường chéo nhau: dùng tích hỗn tạp, thể tích tứ diện hoặc công thức DKH ở mục 10.4.

  

### 11.5. Tính diện tích và thể tích bằng vector

  

- Tam giác:

  $$

  S=\frac12|\vec{AB}\times\vec{AC}|.

  $$

- Hình bình hành:

  $$

  S=|\vec{AB}\times\vec{AD}|.

  $$

- Tứ diện:

  $$

  V=\frac16|\vec{AB}\cdot(\vec{AC}\times\vec{AD})|.

  $$

- Hình hộp:

  $$

  V=|\vec{AB}\cdot(\vec{AD}\times\vec{AA'})|.

  $$

  

## 12. Bảng chọn công cụ

  

| Dạng bài | Công cụ nên nghĩ tới |

|---|---|

| Tính độ dài | Khoảng cách tọa độ, định lí cos, Pythagore |

| Tính góc | Tích vô hướng, vector chỉ phương, pháp tuyến |

| Vuông góc | Tích vô hướng bằng $0$, đường vuông góc mặt phẳng |

| Song song | Vector cùng phương, tích có hướng bằng $\vec0$ |

| Diện tích | Heron, $\frac12 ah$, định thức, tích có hướng |

| Thể tích | $S_{\text{đáy}}h$, tích hỗn tạp |

| Điểm chạy trên đường | Tham số hóa $M=A+t\vec u$ |

| Điểm chạy trên đoạn | $M=A+t(B-A)$, $0\le t\le1$ |

| Điểm trong tam giác | $M=A+s(B-A)+t(C-A)$, $s,t\ge0,s+t\le1$ |

| Cực trị khoảng cách | Hình chiếu, tách vector song song/vuông góc |

| Bài hình không gian khó dựng | Tọa độ hóa đáy trên $Oxy$, chiều cao theo $Oz$ |